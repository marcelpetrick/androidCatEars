<?xml version="1.0" encoding="UTF-8"?>
<!--
  SPDX-FileCopyrightText: 2026 Marcel Petrick <mail@marcelpetrick.it>
  SPDX-License-Identifier: GPL-3.0-or-later

  Android Lint configuration.
  Suppress only when there is a documented reason; never suppress silently.
-->
<lint>
    <!-- ObsoleteSdkInt: minSdk 34; suppress checks for APIs below minSdk -->
    <issue id="ObsoleteSdkInt" severity="ignore" />

    <!--
      OldTargetApi / GradleDependency (compileSdk):
      Android API 37 is not yet available via sdkmanager (repository XML v4
      incompatibility with current cmdline-tools 14742923). Suppressed until
      API 37 platform is installable. Re-enable when sdkmanager can download it.
    -->
    <issue id="OldTargetApi" severity="ignore" />
    <issue id="GradleDependency" severity="ignore" />

    <!--
      NewerVersionAvailable (Kotlin 2.4.0):
      Intentionally pinned to 2.3.21 for KSP 2.3.9 compatibility.
      Upgrade both together when a KSP version for 2.4.x is published.
    -->
    <issue id="NewerVersionAvailable" severity="ignore" />
</lint>
